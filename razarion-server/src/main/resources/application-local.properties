spring.datasource.url=jdbc:mariadb://localhost:32788/razarion
spring.datasource.username=raz_user
spring.datasource.password=1234

jwt.keystore.location=classpath:dev/jwt-dev-keystore.p12
jwt.keystore.password=devpass
jwt.key.alias=jwt
jwt.key.password=devpass

spring.mongodb.uri=mongodb://localhost:27017/razarion

# Director mode (film the live world for social-media clips). DEV-ONLY:
# unset on prod, so DirectorController is never instantiated there.
razarion.director.enabled=true
